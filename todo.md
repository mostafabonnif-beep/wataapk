# قائمة مهام تطبيق الوطنية TV

## 🟢 v8.1.1 — launcher icon جديد وتحكم كامل في أقسام التنقل (جاهزية تقنية؛ الإطلاق الخارجي ما زال معلّقاً)
- [x] تذكيرات البرامج حقيقية: جدولة AlarmManager + إشعار نظام + إعادة جدولة عند إقلاع الجهاز + رفض بدون إذن → بديل مرن
- [x] إعادة تسجيل توكن FCM فوراً عند تجدده (onNewToken) — الإشعارات لا تتوقف بعد دوران التوكن
- [x] النقر على الإشعار يعمل: فتح الشاشة المطلوبة + اختيار قناة البث + فتح الرابط (deep links كاملة)
- [x] استئناف المشاهدة: حفظ الموضع كل 5 ثوانٍ والاستئناف من حيث توقفت (السجل + الأرشيف)
- [x] فحص صحة البث حقيقي: فحص فعلي لاستجابة السيرفر (HTTP + زمن الاستجابة) بدل التخمين
- [x] تيليمتري حقيقي: نبض الأجهزة كل 5 دقائق (devices) + عدّاد المشاهدين النشطين تلقائياً كل دقيقة
- [x] حذف ميزة enableAiSearch الميتة من التطبيق ولوحة التحكم
- [x] حذف قواعد analytics_events غير المستخدمة (استُبدلت بالتيليمتري الحقيقي)
- [x] تشديد قواعد التعليقات (مجاني): createdAt == request.time (server timestamp)، rate limit 30 ثانية عبر users/{uid}.lastCommentAt، رفض أكثر من رابط واحد — اختبارات القواعد **21/21 ناجحة** محلياً وستُشغَّل في CI
- [x] فلتر تعليقات عميلية `ProfanityFilter.kt` (نقل أمين من functions/profanity.js) يُستدعى قبل الإرسال بلا استدعاء شبكي + اختبارات وحدة
- [ ] نشر القواعد: `firebase deploy --only firestore:rules` (مجاني على Spark — بيد المالك بعد `firebase login`)

## ✅ v7.5.0 — إصلاح شامل لقاعدة البيانات والواجهة
- [x] إصلاح خطأ تركيب قواعد Firestore (حلقة for غير مدعومة في لغة القواعد) — ملف القواعد المحلي صالح للاختبار؛ لا يُعد ذلك دليلاً على نشره في المشروع
- [x] التحقق العملي السابق من القواعد عبر محاكي Firestore: 38 اختباراً ناجحاً (قبل آخر تشديد أمني؛ يجب إعادة التشغيل قبل الإطلاق)
- [x] إصلاح تعارض أسماء الحقول: التطبيق كان يرسل installationId بينما القواعد تتطلب deviceId (تعليقات، ملاحظات، توكن FCM) — كلها كانت تُرفض
- [x] توحيد مسار قراءة تفاعلات المشاهدين `live/reactions/user_reactions/{deviceId}` (مع تعطيل الكتابة العميلية للتفاعلات وفق صلاحيات Firestore الحالية)
- [x] إزالة ميزة "البث على الشاشة الذكية" المزيفة (كانت تحاكي الاتصال بتوستات فقط)
- [x] تفعيل جودة البث الحقيقية عبر ExoPlayer track selection (1080p/720p/480p/360p/Auto)
- [x] تشغيل سجل المشاهدة داخل التطبيق بدل المتصفح الخارجي
- [x] إصلاح الحشو المزدوج (double padding) في الهيكل الرئيسي
- [x] تمييز البرنامج "يعرض الآن" في دليل البرامج EPG (يتحدث كل دقيقة)
- [x] ألوان ديناميكية من لوحة التحكم (primaryColor/secondaryColor في config/app)
- [x] حوار "حول التطبيق" برقم الإصدار الحقيقي (بدل v1.0.0 الثابت)
- [x] إصلاح معرّف الإشعارات (overflow في System.currentTimeMillis)
- [x] إغلاق التنبيه المنبثق محلياً فقط (لا يحاول الكتابة كمستخدم عادي)
- [x] جلب الأرشيف والبنرات بترتيب محلي آمن (بدل orderBy الذي يفشل عند نقص حقل)
- [x] إصدار التطبيق: 8.1.1 (versionCode 24)
- [x] استبدال launcher icon بمرجع Android مستقل `ic_launcher_watania` لتجاوز cache الأيقونة القديمة
- [x] إضافة تحكم Firebase في إظهار أقسام الأرشيف والتواصل والمواقع داخل شريط التنقل
- [x] توحيد شعار الوطنية TV في أيقونة Android وSplash وAndroid TV وواجهات التطبيق ولوحة الإدارة
- [x] التحكم عن بعد في اسم التطبيق والشعار النصي ورابط الشعار وصورة الترحيب وألوان Material
- [x] التحكم عن بعد في ظهور معاينة الأرشيف والبنرات الترويجية دون إصدار APK جديد
- [x] إزالة مسارات إعجاب التعليقات والتفاعلات العميلية غير المسموح بها؛ التطبيق يعرض العدادات للقراءة فقط إلى حين توفير جامع server-side موثوق
- [x] فحص شامل سابق: 45/45 اختبار قواعد + بناء + اختبارات وحدة + فحص JavaScript للوحة التحكم (قبل آخر تشديد أمني؛ يجب إعادة تشغيل اختبارات Rules)

## ✅ مكتمل
- [x] البث المباشر مع أزرار تحكم كاملة
- [x] شاشة المواقع الإخبارية
- [x] شاشة YouTube
- [x] شاشة الوسائط الاجتماعية
- [x] شاشة الإعدادات المتقدمة
- [x] حفظ تفضيلات المستخدم
- [x] دعم Firebase للبيانات الحية
- [x] تخزين مؤقت للعمل بدون إنترنت
- [x] مؤشر تحميل WebView
- [x] زر رجوع Android في جميع شاشات WebView
- [~] دعم منصات أخرى خارج نطاق هذا المستودع؛ التطبيق الحالي Android فقط
- [x] أيقونات تبويبات واضحة
- [x] رسائل مناسبة للمستخدم
- [x] Package Name صحيح: com.elwataniatv.app
- [x] البريد الإلكتروني الرسمي: contact@elwataniatv.dz
- [x] إعداد Firebase Hosting بملفات allowlist ومسارات صريحة لـ`/privacy` و`/privacy/` (لا يثبت الإعداد النشر الخارجي)
- [x] حماية رابط البث عبر متغير بيئة
- [x] نظام Push Notifications
- [x] شاشة About / Feedback
- [x] تحسين الواجهة العامة للشاشات الأساسية
- [x] إعدادات اللغة والثيم
- [x] اختبارات الوحدة: `./gradlew test`
- [x] فحص Android lint: `./gradlew lint`
- [~] أصول المتجر: `icon-512.png` و`icon-1024.png` و`feature-graphic.png` موجودة؛ لقطات الشاشة غير موجودة بعد

## ⏳ قبل النشر على Play Store
- [ ] استبدال أيقونات `assets/images/` بالأيقونات الرسمية النهائية إذا رغبت
- [x] رابط الخصوصية التشغيلي هو `https://elwataniatv-channel.vercel.app/privacy`
- [ ] تسجيل حساب Google Play Developer
- [ ] تجهيز توقيع release عبر Gradle properties/متغيرات بيئية محمية (لا keystore أو أسرار في المستودع)
- [ ] بناء AAB: `./gradlew bundleRelease` بعد توفير توقيع release وFirebase production
- [ ] رفع AAB على Google Play Console
- [ ] إضافة لقطات الشاشة للمتجر
- [ ] تعديل رابط تقييم التطبيق بعد النشر
- [ ] ربط `app/google-services.json` بمشروع Firebase production الحقيقي (يدعم Gradle الحالي أيضاً الملف في الجذر؛ مطلوب للمزامنة وFCM)
- [ ] (اختياري) وضع `google-services.json` لـ FCM Push Notifications في بيئة التطوير فقط

## ✅ v7.1.1 — ربط التطبيق بلوحة التحكم بالكامل
- [x] توسيع RemoteAppConfig (كل الحقول إلي تحفظها لوحة التحكم)
- [x] إنشاء use-app-config hook/context لتوفير الإعدادات لكل الشاشات
- [x] ربط اسم القناة والألوان والميزات في شاشة البث المباشر
- [x] إخفاء/إظهار EPG حسب enableEpg من لوحة التحكم
- [x] روابط ديناميكية (فيسبوك، يوتيوب، تليغرام...) في شاشة الإعدادات
- [x] إصلاح duplicate IDs في admin/index.html
- [x] إنشاء seed script لبيانات Firestore الأولية
- [x] تحديث README مع تعليمات الإعداد السريع

## ✅ إعدادات Firebase الواجب عملها يدوياً (مرة واحدة)
- [x] مشروع Firebase production موجود: `elwataniatvapp`
- [ ] إنشاء/تأكيد `.env` المحلي بالقيم الحقيقية (لا تُرفع إلى Git)
- [ ] تأكيد موقع Firestore في Console (السجل يشير إلى `eur3`) — **قرار المنطقة طُبّق في الكود**: كل الدوال على `europe-west1`؛ إن اختلف الموقع في Console، غيّر `REGION` قبل النشر
- [x] Authentication مفعّل: Email/Password + Google + Anonymous (تم تفعيل Anonymous في Console بتاريخ 2026-08-08)
- [ ] تأكيد محتوى نسخة Firestore Rules المنشورة باختبارات Playground/Emulator
- [ ] اختبار ثم نشر النسخة المحلية المطابقة؛ إعداد Firebase Hosting موجود في `admin/firebase.json` ويبني allowlist محلية، لكن لا توجد حالياً صلاحيات لدينا لنشر Hosting أو ربط DNS خارجياً
- [x] حسابا الإدارة موجودان في Authentication؛ ما زال يجب تأكيد custom claim `admin: true`
- [ ] تشغيل `node scripts/set-admin-claim.js <admin-email>` إذا لم يكن الـclaim مضبوطاً
- [ ] تشغيل `node scripts/seed-firestore.js` فقط بعد مراجعة البيانات الإنتاجية الحالية
- [ ] (اختياري لاحقاً) نشر Cloud Functions بعد ترقية Blaze: `firebase deploy --only functions` + `firebase deploy --only firestore:indexes` — القرار الحالي: البقاء على Spark والاعتماد على فلتر العميل + القواعد

## ✅ v7.1.0 — إصلاحات وتحسينات
- [x] إصلاح مشغل YouTube لدعم روابط القنوات من نوع @handle
- [x] إصلاح تمرير دليل البرامج (EPG) مع RTL
- [x] دعم روابط YouTube: /c/channelname و @channelname/live
- [x] إصلاح auto-reconnect: لا يعيد الاتصال إذا المستخدم أوقف البث يدوياً
- [x] إصلاح EPG TodayAt: معالجة آمنة لقيم NaN (دليل البرامج الفاضي)
- [x] إصلاح ContinueWatching: فتح داخل التطبيق بدل الخروج للمتصفح
- [x] مراجعة الاعتماديات وإزالة الحزم غير المستخدمة

## ✅ ملاحظات الإصدار
- [x] Keep Awake أثناء البث
- [x] مراقبة الاتصال وإعادة المحاولة
- [x] شاشة Onboarding
- [x] أرشيف البرامج
- [x] المفضلة
- [x] شاشة الصيانة
- [x] تحسين الثيمات واللغة


## ✅ v7.3.0 — التحسينات الأمنية والهيكلية
- [x] إعادة تسمية الحزمة إلى com.elwataniatv.app (بدل com.example)
- [x] تقييد trust-all SSL على سيرفرات البث ذات المنافذ المخصصة فقط
- [x] إزالة كلمات مرور المسؤول من واجهة التطبيق
- [x] رسالة صادقة لحالة البث في شاشة الإدارة؛ مؤشرات الخدمة الآن مشتقة من الفحص الحقيقي أو تعرض «غير مقاس»
- [ ] نشر لوحة التحكم على Firebase Hosting لاحقاً إذا توفرت صلاحيات Firebase؛ التشغيل الحالي للوحة هو `https://elwataniatv-channel.vercel.app/admin`

## ⏳ مهام المستخدم (Firebase Console)
- [ ] قراءة/اختبار محتوى `admin/firestore.rules` مقابل أحدث إصدار منشور؛ Console يعرض إصداراً بتاريخ 2026-08-08 لكن محرر النص لم يكن قابلاً للقراءة عبر جلسة المتصفح
- [ ] تنظيف بيانات الاختبار من Firestore (مثل سجلات `oussama` أو `200`) بعد مراجعة المالك
- [x] كود App Check جاهز: Play Integrity للـrelease + Debug provider للتطوير (اعتماديات firebase-appcheck/debug)
- [ ] تفعيل App Check Enforcement (خطوات المالك): Play Integrity API في Google Cloud Console → App Check → تسجيل التطبيق → Monitor ثم Enforce على Firestore وAuth — مجاني على Spark
- [ ] تسجيل Debug token في Console للتطوير
- [ ] (اختياري) ترقية Spark → Blaze فقط إذا صارت Functions مطلوبة
- [ ] تقييد مفتاح Firebase Web API في `admin/index.html` بـ HTTP referrers (دومين اللوحة) — انتبه: `appId` الحالي بصيغة android؛ استخدم إعداد Web مخصص وقيّد مفتاح Android بالحزمة+SHA-1 — راجع `admin/DEPLOY.md`
- [ ] الحصول على رابط البث الرسمي للقناة من إدارة القناة


## ✅ v7.4.0 — التكامل والجاهزية
- [x] عداد المشاهدين (من اللوحة) يظهر الآن فوق المشغّل في التطبيق
- [x] سكريبت seed-firestore.js: يضيف EPG والبنرات؛ تنظيف بيانات الاختبار (oussama/200) اختياري عبر `--cleanup-test-data`
- [x] تبويب «المساعدة» في لوحة التحكم (دليل النشر خطوة بخطوة)
- [x] توثيق محدّث (README، FIRESTORE_SECURITY_TASKS) ليعكس قواعد deviceId
