FIREBASE_API_KEY=
FIREBASE_APP_ID=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_PROJECT_ID=
STREAM_URL=

# Optional release signing inputs. Keep blank here; provide via protected
# Gradle properties or CI environment only. Never commit a keystore or secret.
# Local builds may use a protected file path; CI should use the base64 secret below.
RELEASE_STORE_FILE=
RELEASE_KEYSTORE_BASE64=
RELEASE_STORE_PASSWORD=
RELEASE_KEY_ALIAS=
RELEASE_KEY_PASSWORD=