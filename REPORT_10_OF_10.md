# تقرير الوصول بتطبيق الوطنية TV إلى مستوى 10/10

**المشروع:** [Elwataniatv-Channel](https://github.com/merci1994dz/Elwataniatv-Channel)  
**التقنيات:** Kotlin، Jetpack Compose، Material 3، Firebase، Hilt، Room، Media3/ExoPlayer  
**الإصدار الحالي:** `versionCode = 21`، `versionName = 8.0.0`  
**تاريخ التقرير:** 14 أغسطس 2026  
**الحالة الموثقة:** آخر commit مؤكد هو `89de0fb` على الفرع `main`

> **الخلاصة التنفيذية:** أصبح التطبيق أكثر نضجاً واستقراراً واحترافية بوضوح. تم تحسين البنية، الهوية البصرية، البث، EPG، التذكيرات، الإشعارات، الترجمة، RTL/LTR، Android TV، WebView، واختبارات Compose والوحدة. التقييم الواقعي الحالي هو **8.8/10 تقريباً من ناحية الكود والجاهزية**، مع بقاء التحقق على أجهزة حقيقية ونجاح Release build الكامل شرطين أساسيين قبل وصف التطبيق بأنه 10/10 إنتاجياً.

## 1. الهدف ومعيار التقييم

كان الهدف هو تحديث تطبيق الوطنية TV الموجود فعلياً، وليس إنشاء مشروع بديل أو تغيير التقنية. لذلك حافظت جميع التحسينات على Kotlin وJetpack Compose وFirebase وRoom وMedia3، مع إبقاء النماذج والمستودعات ومسارات البيانات الحالية. تم التعامل مع 10/10 باعتباره معياراً مركباً يشمل جودة الكود، تجربة المستخدم، قابلية الترجمة، موثوقية البث، الأمان، الاختبارات، التوافق، وإمكانية النشر.

| محور التقييم | الحالة الحالية | الحكم المهني |
|---|---:|---|
| البنية البرمجية | 8.5/10 | تحسن واضح بعد Hilt وتقسيم ViewModels وفصل المكونات. |
| التصميم وتجربة المستخدم | 8.2/10 | هوية Premium TV وبطاقات وشاشات محسّنة، مع حاجة لاختبار بصري فعلي. |
| البث والمشغل | 8/10 | Media3 ومعالجة أخطاء وإعادة محاولة وجودة قابلة للاختيار؛ يلزم اختبار شبكات حقيقية. |
| EPG والتذكيرات | 8.5/10 | توقيت الجزائر، منتصف الليل، البرنامج التالي، وجدولة التنبيهات محسّنة. |
| الترجمة وRTL/LTR | 8.8/10 | موارد عربية وإنجليزية ودعم رموز اللغة الإقليمية واختبارات وحدة. |
| الإشعارات | 8.2/10 | FCM وقنوات الإشعار وتذكيرات البرامج مترجمة ومتكاملة. |
| Android TV | 8/10 | دعم Leanback والتركيز والتنقل بالريموت محسّن؛ يلزم جهاز TV فعلي. |
| الأمان | 8.2/10 | لا أسرار ثابتة، WebView أكثر تقييداً، و`usesCleartextTraffic=false`. |
| الاختبارات | 7.8/10 | اختبارات وحدة وCompose موجودة، وتحتاج تغطية إنتاجية أوسع. |
| الجاهزية النهائية | 8.5/10 | نجح `assembleDebug` بعد ضبط ذاكرة Gradle؛ ويبقى اختبار الأجهزة وRelease signing قبل النشر. |

## 2. التحسينات المعمارية والبرمجية

تم الانتقال من الاعتماد الكبير على ViewModel مركزي إلى ViewModels منفصلة حسب الشاشة، مثل Live وArchive وEPG وHistory وSettings وMore وNotifications، مع إبقاء البنية رقيقة ومتصلة بالمستودع نفسه. هذا يقلل تشابك الحالة ويجعل اختبار كل شاشة وتطويرها أكثر أماناً.

تم إدخال Hilt للحقن dependency injection بدلاً من إنشاء المستودعات يدوياً داخل ViewModels. أضيفت وحدة المستودع لتوفير `AppDatabase` و`WataniaRepository` بنطاق Singleton، وتم ربط التطبيق وMainActivity وViewModels بالمسار الصحيح. كما تم الحفاظ على Firebase وRoom وMedia3 دون استبدال أي جزء من المكدس التقني.

تم فصل أجزاء MainAppShell إلى مكونات مستقلة تشمل الشريط العلوي، الشريط السفلي، Dialogs، وNavHost. أدى ذلك إلى تقليل التكرار وتسهيل تحسين التنقل والاختبارات، مع الحفاظ على مسارات الشاشات الحالية.

| الملف أو المجال | التحسين |
|---|---|
| `di/RepositoryModule.kt` | توفير قاعدة البيانات والمستودع عبر Hilt. |
| ViewModels | فصل الحالة حسب الشاشة بدلاً من الاعتماد على MainViewModel ضخم. |
| `MainActivity.kt` | ربط ViewModels والتنقل والـShell بطريقة أوضح. |
| `AppTopBar.kt` و`AppBottomBar.kt` | توحيد مظهر التنقل والوصولية. |
| `AppNavHost.kt` وDialogs | فصل المسؤوليات وتقليل التكرار. |
| `gradle/libs.versions.toml` | تثبيت إصدارات Hilt وCompose والاختبارات. |

## 3. الهوية البصرية وتجربة المستخدم

تم اعتماد هوية **Premium TV** بألوان كحلية وحمراء وذهبية، مع رفع التباين وتحسين البطاقات والمساحات والأزرار. لم يعد التصميم يعتمد فقط على تغيير الألوان، بل شمل تحسين التسلسل البصري، وضوح الإجراءات، حالات الفراغ، والتمييز بين المحتوى الحالي والمحتوى القادم.

تم تحسين Hero البث المباشر ليكون أكثر وضوحاً وتوازناً، مع أزرار إجراءات أكبر للوصول إلى EPG والأرشيف ومصادر البث. كما تم تحسين بطاقة البرنامج القادم في الصفحة الرئيسية، وتغيير السهم إلى أيقونة Material متكيفة مع RTL بدلاً من رمز نصي ثابت.

تمت إعادة صقل بطاقات الأرشيف برفع مساحة الصور، تحسين الحشو والتباين، وضبط عناصر التشغيل والمفضلة. كما تم تحسين شرائح EPG وبطاقة البرنامج القادم، ورأس الإعدادات، وبطاقات شاشة المزيد، وشريط التنقل السفلي.

في Android TV، تم الحفاظ على دعم Leanback وإضافة تمييز مرئي لحالة التركيز في شريط التنقل. يظهر العنصر المركّز بالريموت بإطار ذهبي، ما يجعل التنقل بالأسهم أو جهاز التحكم أوضح من مجرد الاعتماد على التركيز غير المرئي.

| الشاشة أو المكوّن | النتيجة البصرية والوظيفية |
|---|---|
| البث المباشر | Hero أوضح، أزرار أكبر، ملخص بث، وبرنامج قادم. |
| EPG | شرائح أكثر توازناً، تذكير واضح، تفاصيل برنامج محسّنة. |
| الأرشيف | صور وبطاقات أوضح ومساحات لمس أفضل. |
| الإعدادات | رأس Premium وحدود ومسافات أكثر اتساقاً. |
| المزيد | بطاقات خدمات بارتفاع وتباين موحدين. |
| التنقل السفلي | دعم focus مرئي للريموت وRTL. |
| WebView | إعدادات أمان أكثر تقييداً وإزالة خصائص deprecated. |

## 4. البث المباشر وMedia3/ExoPlayer

تم الحفاظ على Media3/ExoPlayer مع تحسين واجهة حالات التحميل والتخزين المؤقت والأخطاء. عند فشل الاتصال، يعرض المشغل رسالة مناسبة لحالة الشبكة أو رابط غير موجود أو فشل الاتصال، مع زر إعادة محاولة واضح.

تمت إضافة واختبار آلية اختيار جودة حقيقية باستخدام قيود track selection في ExoPlayer. الأهم أن مفاتيح الجودة الداخلية أصبحت مستقلة عن النص المعروض للمستخدم؛ فاختيار `1080` أو `720` أو `480` أو `360` لا يتأثر بتغيير لغة الواجهة. هذا يمنع خطأ شائعاً كان يمكن أن يحدث عند استخدام النص العربي كقيمة منطقية داخل المشغل.

تم تحسين عناصر التحكم بالصوت، نسبة العرض، السرعة، الشاشة الكاملة، والقفل. كما بقيت حماية YouTube WebView مقيدة بالمضيفات المسموحة وHTTPS، مع منع الوصول إلى الملفات والمحتوى المحلي.

| وظيفة المشغل | الحالة |
|---|---|
| HLS وM3U8 | مدعوم عبر Media3. |
| إعادة المحاولة | موجودة مع رسائل خطأ واضحة. |
| الجودة | اختيار حقيقي مرتبط بقيود المسار، وليس تبديل اسم وهمياً. |
| الصوت والسرعة | متاحان من عناصر تحكم واضحة. |
| الشاشة الكاملة | متاحة عبر Dialog مخصص. |
| YouTube | WebView مقيد بالمضيفات الآمنة وHTTPS. |
| الشبكة الضعيفة | توجد معالجة أولية، ويلزم اختبار ميداني شامل. |

## 5. EPG والمنطق الزمني والتذكيرات

تم إنشاء واستخدام `EpgTime.kt` لتوحيد منطق الوقت في الجزائر بدلاً من الاعتماد المباشر على ساعة الجهاز. يشمل ذلك تحليل أوقات البرامج، اختيار البرنامج الحالي، اختيار البرنامج التالي، حساب الدقائق المتبقية، والتعامل مع الانتقال من نهاية اليوم إلى بداية اليوم التالي.

تم تحسين بطاقة البرنامج القادم لتعرض وقت البداية وتتيح فتح التفاصيل وتفعيل التذكير أو إلغائه. وتم الحفاظ على منطق المستودع وRoom حتى تبقى التذكيرات محلية وقابلة لإعادة الجدولة.

الأهم في طبقة التذكيرات هو أن `ReminderScheduler` أصبح يستخدم منطقة `Africa/Algiers` عند حساب موعد التنبيه، بدلاً من استخدام المنطقة الزمنية المحلية للجهاز دون تمييز. كما تتم إعادة جدولة التذكيرات بعد إقلاع الجهاز عبر `BootReceiver`، وتوجد قناة إشعار مستقلة لتذكيرات البرامج.

| جانب EPG | التحسين |
|---|---|
| المنطقة الزمنية | توقيت الجزائر بدلاً من وقت الجهاز غير الموثوق. |
| منتصف الليل | دعم الانتقال من 23:xx إلى 00:xx. |
| البرنامج الحالي | اختيار مركزي عبر `EpgTime`. |
| البرنامج التالي | ترتيب واختيار مستقل عن ترتيب البيانات الواردة. |
| التذكيرات | حفظ Room وجدولة AlarmManager. |
| Android 12+ | استخدام exact alarm عند السماح، مع fallback windowed alarm. |
| إعادة تشغيل الجهاز | إعادة جدولة التذكيرات المخزنة. |
| اللغة | نصوص التذكير والقناة مترجمة. |

## 6. الترجمة العربية والإنجليزية وRTL/LTR

تم نقل النصوص الثابتة من Composables إلى `values/strings.xml` و`values-ar/strings.xml`. شمل ذلك الشاشات الأساسية والثانوية، Social، CommentSection، Maintenance، Onboarding، Archive، Websites، FirebaseMediaUploader، المشغل، FCM، وتذكيرات البرامج.

تم استخراج دالة نقية من `Theme.kt` لاختيار اتجاه التخطيط. وهي تدعم اللغة العربية الأساسية والرموز الإقليمية مثل `ar-SA` و`ar_DZ`، وتستخدم LTR للإنجليزية واللغات غير المعروفة. هذا يجعل السلوك قابلاً للاختبار دون تشغيل Compose أو جهاز فعلي.

تم أيضاً إصلاح عناصر RTL المرئية، ومنها أسهم التنقل، محاذاة الأزرار، focus في شريط التنقل، وأوصاف الوصول. بقيت أسماء العلامات التجارية وأسماء المنصات وبعض القيم التقنية ثابتة عمداً لأنها هويات أو بيانات تقنية وليست نصوص واجهة قابلة للترجمة.

| الاختبار أو المجال | النتيجة |
|---|---|
| العربية `ar` | RTL |
| العربية الإقليمية `ar-SA` | RTL |
| العربية الإقليمية `ar_DZ` | RTL |
| الإنجليزية `en` | LTR |
| لغة غير معروفة أو فارغة | LTR fallback |
| موارد الإنجليزية | موجودة كـfallback كامل |
| موارد العربية | موجودة ومحدثة للمكونات الرئيسية |

## 7. الإشعارات وFCM

تم تحسين `FcmMessageService` بحيث يستخدم موارد اللغة لقنوات الإشعارات وعنوان الإشعار الاحتياطي. تم فصل قناة الأخبار العاجلة عن القناة العامة، مع ضبط أهمية القناة ووصفها والاهتزاز للأخبار العاجلة.

تم الحفاظ على PendingIntent آمن باستخدام `FLAG_IMMUTABLE` مع توجيه الإشعار إلى الشاشة أو المحتوى المطلوب عبر بيانات FCM. كما تم الحفاظ على تسجيل FCM token عند تدويره.

تمت إزالة النصوص العربية الثابتة من خدمة الإشعارات وقنواتها، وأصبحت أسماء القنوات وأوصافها مرتبطة بموارد Android. كما أُصلحت أخطاء lint الخاصة باستخدام موارد Toast داخل Compose عبر تجهيز النصوص المترجمة في سياق Compose قبل استدعاء callback.

## 8. الأمان وWebView والبيانات

تم إزالة الاعتماد على الأسرار الثابتة من الكود، والحفاظ على Firebase كخلفية التطبيق دون وضع مفاتيح أو كلمات مرور أو روابط قاعدة بيانات حساسة داخل الملفات المصدرية. تم الإبقاء على `android:allowBackup=false` و`android:usesCleartextTraffic=false`، مع التصريح بالصلاحيات المطلوبة فقط.

تم تشديد إعدادات WebView في `WebBrowserView.kt` عبر تعطيل `allowFileAccess` و`allowContentAccess`، منع المحتوى المختلط، منع النوافذ المتعددة، وتعطيل خصائص JavaScript غير الضرورية. كما أزيلت خصائص deprecated الخاصة بالوصول من file URLs، ما أزال تحذيرات WebView السابقة.

في YouTube WebView، تم قصر الروابط المسموحة على HTTPS ومضيفات YouTube الرسمية أو النطاقات التابعة لها. هذا يقلل احتمال فتح محتوى خارجي غير موثوق من داخل المتصفح المضمّن.

## 9. لوحة الإدارة ورفع الصور

تم الحفاظ على لوحة الإدارة الحالية وعدم تحويلها إلى React Native أو مشروع بديل. تم تحسين مكوّن `FirebaseMediaUploader` بإزالة نصوصه الثابتة وإضافة موارد عربية وإنجليزية كاملة.

تستخدم الواجهة الآن روابط HTTPS خارجية للصور عند الحاجة، وتعرض للمستخدم بوضوح أن الملفات لا تُرفع إلى Firebase Storage في هذا المسار. كما يتم رفض الروابط التي لا تبدأ بـHTTPS، وتوجد معاينة للصورة ورسائل نجاح وفشل مترجمة.

| الوظيفة | الحالة |
|---|---|
| معاينة الصورة | موجودة مع وصف وصول. |
| رابط خارجي مجاني | مدعوم ومشرح للمستخدم. |
| التحقق من HTTPS | موجود. |
| رسائل النجاح والفشل | مترجمة. |
| Firebase Storage | لا يتم استخدامه في هذا المسار الخارجي. |
| النصوص الثابتة | نُقلت إلى الموارد. |

## 10. الاختبارات المضافة والموجودة

تمت إضافة اختبارات وحدة واختبارات Compose إلى المشروع. تشمل اختبارات الوحدة منطق EPG والتوقيت، اللغة والاتجاه، سلامة التفاعل والكتابة، النماذج، المستودع، الإشعارات، تشغيل الوسائط، وتصفية الكلمات غير المناسبة.

وتشمل اختبارات Android/Compose شاشات Live وArchive وNavigation وLanguageSelector وPremiumLiveHero. تم إصلاح استخدام قاعدة Compose إلى `createComposeRule` بما يتوافق مع إصدار الاختبارات الموجود في المشروع.

| نوع الاختبار | التغطية الحالية |
|---|---|
| Unit tests | EPG، RTL، النماذج، المستودع، الإشعارات، الأمان، التفاعل. |
| Compose tests | Live، Archive، Navigation، LanguageSelector، PremiumLiveHero. |
| RTL unit tests | العربية الأساسية، العربية الإقليمية، LTR fallback. |
| Lint | تم تشغيله بنجاح في الفحوصات السابقة. |
| Compile Kotlin | تم تشغيله بنجاح بعد آخر دفعات رئيسية. |
| اختبار جهاز فعلي | ما زال مطلوباً قبل إعلان الجاهزية الإنتاجية الكاملة. |

## 11. سلسلة الـcommits الرئيسية

تم تنفيذ العمل على دفعات صغيرة قابلة للمراجعة والدفع، بدلاً من commit واحد ضخم يصعب التحقق منه.

| Commit | الوصف |
|---|---|
| `cbac3f9` | تغطية رموز اللغة الإقليمية في اختبارات RTL. |
| `8f82137` | تشديد إعدادات WebView وإزالة الخصائص deprecated. |
| `524ef24` | تعريب قنوات إشعارات FCM. |
| `eca4ba6` | تعريب تذكيرات EPG وربطها بتوقيت الجزائر. |
| `d5b8408` | تحسين focus والتنقل بالريموت في Android TV. |
| `92134f9` | تعريب FirebaseMediaUploader وواجهة الإدارة. |

آخر commit مؤكد على GitHub هو `89de0fb`، والفرع `main` كان نظيفاً ومتزامناً عند آخر دفع موثق.

## 12. نتائج التحقق والقيود

نجحت عمليات `compileDebugKotlin` و`lintDebug` و`testDebugUnitTest` في جلسات التحقق الموثقة. كما نجحت فحوصات XML و`git diff --check`، ولم تظهر أخطاء موارد بعد إضافة الترجمات الجديدة. وتم إصلاح ثلاث مخالفات lint في `FirebaseMediaUploader` مرتبطة بقراءة موارد النصوص من `LocalContext.current`.

بعد ضبط ذاكرة Gradle وKotlin daemon، نجحت عملية `assembleDebug` الكاملة في 4 دقائق و9 ثوانٍ، وشملت lint والاختبارات وتجميع APK. ظهرت ملاحظة غير مانعة حول عدم إمكانية strip لبعض مكتبات native، وتم تغليفها كما هي دون فشل البناء.

> لا يجوز اعتبار نجاح الترجمة أو lint وحده دليلاً على أن البث يعمل على كل جهاز. الجاهزية 10/10 الإنتاجية تتطلب APK كاملاً، تثبيته، اختباره على هاتف وAndroid TV، وتجربة البث الفعلي مع شبكة ضعيفة.

## 13. الفجوات المتبقية للوصول إلى 10/10 إنتاجية

رغم أن الأساس البرمجي أصبح قوياً، فإن هناك مجموعة من الخطوات التي لا يمكن إثباتها من قراءة الكود وحدها. الأولوية الأولى هي إعادة تشغيل Release/Debug build كامل في بيئة مستقرة بذاكرة كافية، ثم تثبيت APK على أجهزة فعلية.

يجب اختبار مصادر HLS متعددة، فقدان الاتصال، إعادة الاتصال، الانتقال بين الخادم الأساسي والبديل، استهلاك البطارية، وتبديل الجودة أثناء التشغيل. كما يجب اختبار التذكيرات عند اختلاف المنطقة الزمنية، بعد إعادة تشغيل الجهاز، ومع صلاحيات Android 12 وAndroid 13 وAndroid 14.

يحتاج Android TV إلى اختبار فعلي بالريموت، خصوصاً ترتيب focus والتنقل بين البطاقات والمشغل وشريط التنقل. كما ينبغي اختبار أحجام الشاشات والكثافة واتجاه RTL على جهاز فعلي أو محاكي TV.

أما لوحة الإدارة فتحتاج في المرحلة الإنتاجية إلى تدقيق أدوار وصلاحيات، سجل تغييرات، تحقق من روابط البث قبل النشر، ومعاينة إشعارات قبل إرسالها. ويستحسن أيضاً إضافة Crash Reporting ومراقبة آمنة لفشل البث بعد الإطلاق.

| الأولوية | الإجراء المتبقي | سبب الأهمية |
|---:|---|---|
| 1 | توقيع `assembleRelease` واختبار APK على أجهزة حقيقية | إكمال اعتماد APK الإنتاجي بعد نجاح `assembleDebug`. |
| 2 | اختبار هاتف وAndroid TV حقيقيين | التحقق من التصميم وfocus والبث. |
| 3 | اختبار بث وشبكات ضعيفة | اكتشاف مشاكل لا تظهر في compile أو lint. |
| 4 | اختبار إشعارات Android الحديثة | ضمان ظهور FCM والتذكيرات للمستخدم. |
| 5 | Crash Reporting ومراقبة البث | حماية ما بعد النشر. |
| 6 | مراجعة صلاحيات لوحة الإدارة | تقليل مخاطر النشر الخاطئ أو غير المصرح. |

## 14. طريقة إعادة التحقق المقترحة

من أجل إعادة التحقق في بيئة CI أو جهاز تطوير، يجب استخدام JDK كامل يحتوي `javac`، وAndroid SDK 36، وBuild Tools مناسبة، ثم تنفيذ الأوامر التالية من جذر المشروع:

```bash
./gradlew :app:lintDebug
./gradlew :app:testDebugUnitTest
./gradlew :app:compileDebugKotlin
./gradlew :app:assembleDebug
```

بعد نجاحها، يجب تثبيت APK على جهاز اختبار، تفعيل العربية والإنجليزية، تجربة RTL، تشغيل البث، فتح EPG، تفعيل تذكير، إعادة تشغيل الجهاز، ثم اختبار الإشعار. وينبغي تسجيل نتيجة كل سيناريو في قائمة قبول قبل إطلاق النسخة.

## 15. الحكم النهائي

التطبيق حالياً **نسخة احترافية متقدمة وقريبة من مستوى 10/10** من ناحية البنية والواجهة والترجمة ومنطق EPG والتكاملات الأساسية. أهم ما يميزه أنه تم تطويره داخل المشروع الحقيقي وبالتقنيات الأصلية، مع الحفاظ على Firebase وMedia3 وRoom وعدم إدخال أسرار أو تغيير جذري غير مطلوب.

التقييم الواقعي الحالي هو **8.8/10 تقريباً**. أصبح `assembleDebug` وlint واختبارات الوحدة ناجحاً بعد ضبط الذاكرة وإصلاح أخطاء lint. يمكن رفعه إلى 9.5 ثم 10/10 إنتاجي عبر توقيع Release، إكمال التحقق على أجهزة حقيقية، اختبار البث الواقعي، ضبط Android TV ميدانياً، إضافة مراقبة الأخطاء، واستكمال تدقيق صلاحيات لوحة الإدارة.

> **النتيجة:** تم إنجاز معظم العمل البرمجي والمرئي الأساسي للوصول إلى 10/10. أما الجزء المتبقي فهو في معظمه تحقق إنتاجي وتشغيلي لا يمكن ضمانه من داخل sandbox وحدها، ولا ينبغي الادعاء بإتمامه قبل تشغيل التطبيق فعلياً على الأجهزة والشبكات المستهدفة.

## المراجع

[1]: https://github.com/merci1994dz/Elwataniatv-Channel "مستودع تطبيق الوطنية TV على GitHub"
[2]: https://github.com/merci1994dz/Elwataniatv-Channel/blob/main/app/build.gradle.kts "إعدادات بناء تطبيق Android وإصدار التطبيق"
[3]: https://github.com/merci1994dz/Elwataniatv-Channel/blob/main/app/src/main/AndroidManifest.xml "AndroidManifest والصلاحيات وخدمات الإشعارات"
[4]: https://github.com/merci1994dz/Elwataniatv-Channel/tree/main/app/src/test "اختبارات الوحدة"
[5]: https://github.com/merci1994dz/Elwataniatv-Channel/tree/main/app/src/androidTest "اختبارات Compose وAndroid"
[6]: https://github.com/merci1994dz/Elwataniatv-Channel/commits/main "سجل commits والتحسينات المدفوعة"
[7]: https://developer.android.com/studio/write/lint "توثيق Android lint وفحوصات جودة الكود"
