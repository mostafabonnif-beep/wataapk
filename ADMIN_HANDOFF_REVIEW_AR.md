# مراجعة تقرير التسليم — Elwatania TV v8.5.0

**تاريخ المراجعة:** 25 أغسطس 2026

**المستودع:** `mostafabonnif-beep/wataapk`

**الفرع:** `main`

## النتيجة التنفيذية

تمت مطابقة تقرير `ADMIN_HANDOFF_REPORT_AR.md` مع حالة المستودع. كانت أغلب النقاط التقنية متوافقة مع المشروع، لكن وُجدت نقطتان تحتاجان إلى تصحيح: سكريبت تنظيف البيانات لم يكن موجوداً في النسخة المحلية وقت المراجعة، كما أن إصلاح CI الخاص بتخطي بناء Hosting عند غياب أسرار Firebase لم يكن مثبتاً بالكامل في النسخة المحلية. تمت معالجة النقطتين ودفع الإصلاحات إلى GitHub.

## الإصلاحات المنفذة

| المجال | الإجراء | الحالة |
|---|---|---|
| تنظيف بيانات الاختبار | إضافة `scripts/cleanup-test-data-only.js`، وهو سكريبت اختياري لا يعمل إلا مع تأكيد صريح، ويستهدف مستندات EPG والإشعار والخبر العاجل المحددة في التقرير فقط | مكتمل |
| حماية التنظيف | رفض التشغيل دون `CONFIRM_TEST_DATA_CLEANUP=YES`، وقراءة حساب الخدمة من مسار محلي غير متعقب فقط | مكتمل |
| CI Hosting | جعل التخطي عند غياب أسرار Firebase اختيارياً عبر `ALLOW_HOSTING_BUILD_SKIP=true` | مكتمل |
| تحقق CI | تخطي فحص allowlist فقط عند عدم إنشاء `admin/hosting-dist`، مع بقاء الفحص صارماً عندما يُبنى الناتج | مكتمل |
| لوحة الإدارة | تبويب Firebase مخفي من التنقل العام، مع إبقاء التهيئة الداخلية وتزامن Firestore دون حذف | مكتمل |

## التحقق

تم التأكد من عدم وجود ملفات حساب خدمة أو `google-services.json` أو keystore متعقبة في Git. كما اجتازت ملفات JavaScript الجديدة والمعدلة فحص الصياغة، واجتازت شجرة المشروع `git diff --check`.

أما تطبيق Android فقد اجتاز في الدفعات السابقة:

| الاختبار | النتيجة |
|---|---|
| `compileDebugKotlin` | ناجح |
| `testDebugUnitTest` | ناجح |
| `lintDebug` | ناجح |

آخر commit منشور هو `c6c1f60`، وهو موجود على كل من `HEAD` و`origin/main`.

## طريقة تشغيل تنظيف البيانات — بعد المراجعة اليدوية فقط

لا يجب تشغيل السكريبت إلا بعد التأكد من أن المستندات المحددة تجريبية فعلاً، وبعد تنزيل حساب خدمة جديد أو محمي من Firebase Console. يوضع الملف خارج Git، ثم يُشغّل محلياً بالأمر التالي:

```bash
GOOGLE_APPLICATION_CREDENTIALS=/مسار/محمي/service-account.json \
CONFIRM_TEST_DATA_CLEANUP=YES \
node scripts/cleanup-test-data-only.js
```

السكريبت لا يقرأ أو يحذف مجموعة كاملة، ولا ينفذ أي عملية تنظيف شاملة. لم يتم تشغيله أثناء هذه المراجعة لأن تشغيله يعد تعديلاً مباشراً لبيانات Firestore ويتطلب تأكيداً صريحاً من صاحب المشروع بعد المعاينة.

## نقاط تتطلب إجراءً من صاحب الحساب

تفعيل GitHub Pages، إضافة أسرار CI، إنشاء أو تدوير حساب الخدمة، التأكد من رابط البث الرسمي، إدخال ترددات الأقمار الصناعية، ورفع إصدار Play Store كلها عمليات تتطلب دخول صاحب الحساب أو موافقته. لا يمكن تنفيذها بأمان من خلال المستودع وحده.

## تنبيه أمني مهم

الملف المرفق يحتوي على بيانات اعتماد حساسة بصيغة صريحة، بما في ذلك بيانات دخول إدارية وبيانات مرتبطة بالتوقيع. يجب عدم نشر التقرير أو مشاركته كما هو. يوصى فوراً بتغيير كلمة مرور الحساب الإداري، وتدوير أي بيانات اعتماد ظهرت في التقرير، وحفظ نسخة منقحة لا تحتوي على أسرار. لم تُنسخ هذه القيم إلى المستودع أو إلى هذا التقرير.

## الخلاصة

المستودع متزامن ونظيف، والإصلاحات المتعلقة بتقرير التسليم منشورة على `origin/main`. بقيت عمليات Firestore وGitHub Pages وSecrets وPlay Console خارج التنفيذ التلقائي لأنها عمليات حساسة تتطلب حساب المالك وتأكيده المباشر.
